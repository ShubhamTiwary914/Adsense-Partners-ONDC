import { Box } from "@mui/material"


export default function SupportTab({ fields, initialData, handleSettingsUpdate, formTitle }){

    return (
        <Box sx={{ width: '85%', border: `1px solid black`, borderRadius: 1, p:2 }}>
            Work in Progress
        </Box>
    )
}